--- a/original_nurbs_preprocessing/edgeline_extraction_headless.cpp
+++ b/original_nurbs_preprocessing/edgeline_extraction_headless.cpp
@@ -573,7 +573,7 @@
 	if (num_points < 50) {
-		max_radius = 0.015;  // 15mm for very sparse data (pottery rims)
+		max_radius = 15.0;  // Juglet ticket 02: mm (was meters-assumed)
 	} else if (num_points < 100) {
-		max_radius = 0.010;  // 10mm for sparse data
+		max_radius = 10.0;  // Juglet ticket 02: mm
 	} else {
-		max_radius = 0.005;  // 5mm for dense data (original)
+		max_radius = 5.0;  // Juglet ticket 02: mm
 	}
